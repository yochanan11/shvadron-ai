# יוחנן הדיגיטלי — בוט השאלות של עמוד הקורס

בוט שאלות ותשובות בעמוד `course.html`. עונה בעברית על שאלות לגבי הקורס
הדיגיטלי בלבד, על סמך מאגר ידע סגור, ומפנה לוואטסאפ כשאין לו תשובה מבוססת.

---

## הקבצים

| קובץ | תפקיד |
|---|---|
| `assets/js/course-pricing.js` | **מקור האמת היחיד למחירים.** נטען גם בדפדפן וגם בשרת. |
| `assets/css/course-bot.css` | העיצוב של הרכיב. משתמש במשתני העיצוב של האתר. |
| `assets/js/course-bot.js` | הרכיב עצמו: כפתור, בועת הזמנה, חלון שיחה, שליחה, שגיאות. |
| `api/_course-kb.js` | **מאגר הידע** וה-System Prompt. |
| `api/course-bot.js` | נקודת הקצה בשרת: ולידציה, הגבלת קצב, קריאה למודל. |

הרכיב נטען מ-`course.html` בלבד. אם `assets/js/course-bot.js` לא נטען,
העמוד עובד כרגיל בלי הבוט.

---

## הפעלה

הבוט עובד ברגע שמוגדר מפתח API. בלעדיו הוא עדיין נטען, אומר בעברית שאין
לו תשובה, ומציע מעבר לוואטסאפ — העמוד לא נשבר.

### משתני סביבה

| משתנה | חובה | ברירת מחדל | תיאור |
|---|---|---|---|
| `ANTHROPIC_API_KEY` | **כן** | — | מפתח ה-API. כבר בשימוש ב-`api/chat.js`, כך שאם הוא מוגדר בפרויקט ב-Vercel אין מה לעשות. |
| `COURSE_BOT_MODEL` | לא | `claude-opus-5` | הדגם. אפשר להחליף ל-`claude-haiku-4-5` לתגובה מהירה וזולה יותר. |
| `COURSE_BOT_RPM` | לא | `15` | מספר שאלות מותר לכל גולש בחלון של 10 דקות. |
| `KV_REST_API_URL`, `KV_REST_API_TOKEN` | לא | — | אם מוגדרים (הם כבר בשימוש למנגנון הקופונים), הגבלת הקצב משותפת לכל המכונות. אחרת היא נשמרת בזיכרון של כל מכונה בנפרד. |

הגדרה ב-Vercel: Project → Settings → Environment Variables.
**אין להכניס את המפתח לשום קובץ בריפו.** כל הפנייה למודל מתבצעת בשרת בלבד.

### מקומית

```bash
npm install
# .env בשורש הפרויקט:
#   ANTHROPIC_API_KEY=sk-ant-...
npm start          # http://localhost:3000/course.html
```

---

## עדכון מאגר הידע

כל מה שהבוט מותר לו לומר נמצא ב-`api/_course-kb.js`, במערך `KNOWLEDGE`.

1. מוסיפים או משנים שורה בעברית, בניסוח עובדתי, תחת הכותרת המתאימה.
2. **מחירים ומע״מ לא נכתבים שם.** הם נשאבים מ-`assets/js/course-pricing.js`.
3. תוכן חדש שנוסף ל-`course.html` צריך להתווסף גם למאגר הידע, אחרת הבוט
   לא יידע עליו ויפנה לוואטסאפ.
4. `USE_CASE_PALETTE` באותו קובץ מגדיר את השימושים המעשיים שהבוט רשאי
   להציע לגולש לפי התחום שלו. אין להוסיף שם יכולת שלא נלמדת בקורס.
5. אחרי כל שינוי — deploy. אין מטמון נפרד למאגר הידע.

### מה אסור להוסיף למאגר

הבטחות תוצאה או הכנסות · מספרי חיסכון בזמן · תאריכים ומועדי פתיחה ·
תנאי ביטול שאינם בעמוד `cancellation.html` · הבטחות לתמיכה או ליווי אישי
בגרסה הדיגיטלית · כל דבר שלא מופיע בעמוד הקורס.

---

## שינוי מחיר

**מקום אחד בלבד:** `assets/js/course-pricing.js`.
משם נגזרים: המחירים בעמוד, המחיר שהבוט אומר, הנתונים המובנים ל-Google,
ובסיס חישוב ההנחה ב-`api/coupon.js`.

לאחר שינוי מחיר יש לעדכן גם את קישור הסליקה (`checkoutUrl` באותו קובץ),
כך שהוא יגבה בדיוק את הסכום שהאתר מציג.

---

## פרטיות ואנליטיקה

* אירועים נשלחים דרך `gtag` בלבד, שהוא no-op עד לאישור קטגוריית
  "סטטיסטיקה" בהודעת העוגיות. כל עוד הגולש לא הכריע, הרכיב כלל אינו מוצג.
* **תוכן השאלות לא נשלח לאנליטיקה.** האירועים:
  `bot_open`, `bot_send_message` (רק אורך מקוצר: short/long),
  `bot_quick_question` (רק מספר סידורי), `bot_click_buy`, `bot_click_whatsapp`.
* השיחה נשמרת ב-`sessionStorage` של הדפדפן בלבד ונמחקת בסגירת הלשונית.
  היא לא נשמרת בשרת.
* ה-IP משמש להגבלת קצב אחרי גיבוב חד-כיווני (SHA-256), ואינו נשמר כפי שהוא.
* ללוגים בשרת נכתב רק סוג התקלה — לא תוכן השאלה ולא פרטי הגולש.
